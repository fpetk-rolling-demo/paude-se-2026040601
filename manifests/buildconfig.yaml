apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: paude-se-2026040601
  namespace: rhdh-app
  labels:
    app: paude
    paude.io/session-name: paude-se-2026040601
spec:
  source:
    type: Git
    git:
      uri: https://github.com/fpetk-rolling-demo/paude-se-2026040601
      ref: main
    contextDir: build
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: paude-se-2026040601:latest
  triggers:
    - type: ConfigChange
